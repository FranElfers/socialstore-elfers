export default function CartWidget() {
	return <a title='Carrito' id='carrito' href="#">🛒</a>
}

// Este componente lo unico que tiene que tener es un icono
// Este icono tiene que estar adentro del navbar que ya creamos
// Osea el componente NavBar tiene que adentro usar CartWidget
// Estilizar